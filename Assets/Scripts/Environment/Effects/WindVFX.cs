using System.Collections;
using UnityEngine;

namespace Assets.Scripts.Environment.Effects
{
    public class WindVFX : MonoBehaviour
    {
        [SerializeField]
        ParticleSystem[] _Effects;
        // what can we control?
        // direction
        // width
        // intensity (optional)


        // random material (small/large wind) alternation

        // Use this for initialization
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}